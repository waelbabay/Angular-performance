import { SaleMessagePipe } from './sale-message.pipe';

describe('SaleMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new SaleMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
